import {Bot,LockKeyhole,ShieldCheck} from "lucide-react";import {useState} from "react";import {loginBot} from "../services/botApi.js";import "./BotLogin.css";
export default function BotLogin({onAuthorized}){const [key,setKey]=useState(""),[error,setError]=useState(""),[busy,setBusy]=useState(false);const submit=async e=>{e.preventDefault();setBusy(true);setError("");try{await loginBot(key);onAuthorized?.()}catch(x){setError(x.message==="BOT_ACCESS_DENIED"?"Access key not accepted.":x.message)}finally{setBusy(false)}};return <main className="bot-login"><form onSubmit={submit}><div className="bot-login-mark"><Bot size={26}/></div><span>AEMA PRIVATE EXECUTION</span><h1>Bot Access</h1><p>This workspace contains paper account, position and execution telemetry. A separate authorization is required.</p><label><LockKeyhole size={15}/>Bot access key</label><input type="password" value={key} onChange={e=>setKey(e.target.value)} autoComplete="current-password" required autoFocus/><button disabled={busy}>{busy?"Verifying…":"Unlock Bot"}</button>{error&&<div className="bot-login-error">{error}</div>}<footer><ShieldCheck size={13}/>Protected separately from the research workspace</footer></form></main>}
