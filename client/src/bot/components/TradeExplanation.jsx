import {useEffect,useState} from "react";
import {getTradeExplanation} from "../services/botApi.js";
import "./TradeExplanation.css";
const view=v=>v===null||v===undefined?"Not recorded":typeof v==="object"?JSON.stringify(v,null,2):String(v);
export default function TradeExplanation({id,onClose}){const [data,setData]=useState(null),[error,setError]=useState("");useEffect(()=>{let alive=true;setData(null);setError("");getTradeExplanation(id).then(x=>alive&&setData(x)).catch(e=>alive&&setError(e.message));return()=>{alive=false}},[id]);return <div className="bot-explanation-backdrop" role="presentation" onClick={onClose}><section className="bot-explanation" role="dialog" aria-modal="true" aria-label="Trade explanation" onClick={e=>e.stopPropagation()}><header><div><span>RECORDED EVIDENCE</span><h2>Why this trade?</h2></div><button onClick={onClose} aria-label="Close explanation">×</button></header>{error&&<p className="bot-error">{error}</p>}{!data&&!error&&<p>Loading recorded evidence…</p>}{data&&<><h3>{data.symbol} · {data.direction==="LONG"?"Bullish Direction":data.direction==="SHORT"?"Bearish Direction":"Direction unavailable"}</h3>{data.evidenceMissing&&<p className="bot-explanation-warning">Entry snapshot was not recorded for this trade. No explanation has been invented.</p>}<div className="bot-explanation-metrics"><span>Research rank: {view(data.researchRankScore)}</span><span>Execution rank: {view(data.executionRankScore)}</span><span>Exit reason: {view(data.management?.exitReason)}</span></div><h4>Original entry research</h4><pre>{view(data.entrySnapshot)}</pre><h4>Direction decision</h4><pre>{view(data.entryDirectionDecision)}</pre><h4>Position management and trailing</h4><pre>{view(data.management)}</pre><h4>Consistency history</h4><pre>{view(data.consistencyHistory)}</pre><h4>Recorded closed-trade result</h4><pre>{view(data.result)}</pre><small>Explanations use recorded evidence only. Missing evidence remains unavailable.</small></>}</section></div>}
