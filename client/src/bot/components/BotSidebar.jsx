import {Bot,History,LayoutDashboard,LogOut,SlidersHorizontal} from "lucide-react";
import {NavLink} from "react-router-dom";import {logoutBot} from "../services/botApi.js";import "./BotSidebar.css";
export default function BotSidebar(){const items=[["Dashboard","/bot",LayoutDashboard],["Capital & Controls","/bot/controls",SlidersHorizontal],["Trade History","/bot/history",History]];return <aside className="bot-side"><div className="bot-brand"><Bot size={20}/><div><strong>AEMA BOT</strong><span>PRIVATE · PAPER</span></div></div><nav>{items.map(([l,to,I])=><NavLink key={to} end={to==="/bot"} to={to} className={({isActive})=>isActive?"active":""}><I size={17}/><span>{l}</span></NavLink>)}</nav><button onClick={async()=>{await logoutBot();location.href="/bot"}}><LogOut size={16}/>Lock Bot</button></aside>}
